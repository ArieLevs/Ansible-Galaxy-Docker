galaxy_info:
  role_name: docker
  author: Arie Lev
  description: Docker setup for Raspberry Pi and CentOS
  license: license (MIT)
  min_ansible_version: 2.9.6
  platforms:
    # Supported platforms list here https://galaxy.ansible.com/api/v1/platforms/
    - name: EL
      versions:
        - 7
        - 8
    - name: Debian
      versions:
        - stretch
        - buster

  galaxy_tags:
    - docker
    - raspberry
    - centos

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
